# FinSys Application Configuration
# Base URL of the application
base.url=http://localhost:90/finsys

# Browser (chrome / firefox)
browser=chrome

# Application credentials
app.username=dummycfo
app.password=passw0rd

# Wait timeouts (in seconds)
implicit.wait=10
explicit.wait=10
